export const CACHE_BUSTER = Date.now().toString().slice(-5);
export const ASSETS_MOUNT_POINT = 'a';
export const ASSETS_PATH = '../../assets';
// Used to lookup in the client-meta.json file from esbuild
export const CLIENT_ENTRY_POINT = 'client/app.ts';

export const INDEX_PAGE_ID = '__index__';
